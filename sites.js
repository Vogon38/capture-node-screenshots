const { link } = require("fs");

 const urls = [
        { name: 'Facebook', link: 'https://www.facebook.com' },
    ];

console.table(urls);
console.log('------------------------------------------------------------------------------------------------------')

module.exports = {urls};
